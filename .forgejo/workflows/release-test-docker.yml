name: Release Test - Docker to Scaleway (Kaniko/Crane)

on:
    workflow_dispatch:

jobs:
    docker-build-and-push-test:
        runs-on: codeberg-medium
        container:
            image: gcr.io/kaniko-project/executor:debug
        steps:
            - name: Resolve build context path
              run: |
                  set -euo pipefail
                  if [ -n "${GITHUB_WORKSPACE:-}" ] && [ -f "${GITHUB_WORKSPACE}/Dockerfile" ]; then
                    echo "CONTEXT_DIR=${GITHUB_WORKSPACE}" >> "$GITHUB_ENV"
                    exit 0
                  fi
                  if [ -f "/workspace/CE-RISE-software/hex-core-service/Dockerfile" ]; then
                    echo "CONTEXT_DIR=/workspace/CE-RISE-software/hex-core-service" >> "$GITHUB_ENV"
                    exit 0
                  fi
                  if [ -f "/workspace/Dockerfile" ]; then
                    echo "CONTEXT_DIR=/workspace" >> "$GITHUB_ENV"
                    exit 0
                  fi
                  echo "Could not find Dockerfile in known workspace paths."
                  ls -la /
                  ls -la /workspace || true
                  exit 1

            - name: Compute image tags
              id: meta
              run: |
                  set -euo pipefail
                  SHORT_SHA="$(printf '%s' "${GITHUB_SHA}" | cut -c1-12)"
                  echo "SHORT_SHA=${SHORT_SHA}" >> "$GITHUB_OUTPUT"
                  echo "TEST_TAG=test-${GITHUB_RUN_NUMBER}" >> "$GITHUB_OUTPUT"

            - name: Build and push test image with Kaniko
              env:
                  SHORT_SHA: ${{ steps.meta.outputs.SHORT_SHA }}
                  TEST_TAG: ${{ steps.meta.outputs.TEST_TAG }}
                  REGISTRY_HOST: ${{ vars.REGISTRY_HOST }}
                  REGISTRY_NAMESPACE: ${{ vars.REGISTRY_NAMESPACE }}
                  REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
                  REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
              run: |
                  set -euo pipefail
                  IMAGE_BASE="${REGISTRY_HOST}/${REGISTRY_NAMESPACE}/hex-core-service"
                  AUTH="$(printf '%s:%s' "${REGISTRY_USERNAME}" "${REGISTRY_PASSWORD}" | base64 | tr -d '\n')"
                  mkdir -p /kaniko/.docker
                  cat > /kaniko/.docker/config.json <<EOF
                  {"auths":{"${REGISTRY_HOST}":{"auth":"${AUTH}"}}}
                  EOF
                  /kaniko/executor \
                    --context "${CONTEXT_DIR}" \
                    --dockerfile "${CONTEXT_DIR}/Dockerfile" \
                    --destination "${IMAGE_BASE}:${SHORT_SHA}" \
                    --destination "${IMAGE_BASE}:${TEST_TAG}"

    docker-tag-test-latest:
        runs-on: codeberg-small
        needs:
            - docker-build-and-push-test
        container:
            image: gcr.io/go-containerregistry/crane:debug
        steps:
            - name: Compute image tags
              id: meta
              run: |
                  set -euo pipefail
                  SHORT_SHA="$(printf '%s' "${GITHUB_SHA}" | cut -c1-12)"
                  echo "SHORT_SHA=${SHORT_SHA}" >> "$GITHUB_OUTPUT"
                  echo "TEST_TAG=test-${GITHUB_RUN_NUMBER}" >> "$GITHUB_OUTPUT"

            - name: Tag test-latest with Crane
              env:
                  SHORT_SHA: ${{ steps.meta.outputs.SHORT_SHA }}
                  TEST_TAG: ${{ steps.meta.outputs.TEST_TAG }}
                  REGISTRY_HOST: ${{ vars.REGISTRY_HOST }}
                  REGISTRY_NAMESPACE: ${{ vars.REGISTRY_NAMESPACE }}
                  REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
                  REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}
              run: |
                  set -euo pipefail
                  IMAGE_BASE="${REGISTRY_HOST}/${REGISTRY_NAMESPACE}/hex-core-service"
                  crane auth login -u "${REGISTRY_USERNAME}" -p "${REGISTRY_PASSWORD}" "${REGISTRY_HOST}"
                  crane copy "${IMAGE_BASE}:${SHORT_SHA}" "${IMAGE_BASE}:test-latest"
                  echo "Pushed:"
                  echo "${IMAGE_BASE}:${SHORT_SHA}"
                  echo "${IMAGE_BASE}:${TEST_TAG}"
                  echo "${IMAGE_BASE}:test-latest"
