//! hex-cli — command-line interface for the CE-RISE Hex Core Service.
//!
//! Two modes of operation (selected via subcommand):
//!   - library mode: calls core use cases directly (no HTTP)
//!   - client mode:  calls the REST API over HTTP
//!
//! TODO: implement subcommands using `clap` once added to workspace dependencies.

fn main() {
    println!("hex-cli: not yet implemented");
}

